{
  "name": "gemeinsam-wirkt-web",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "search": "pagefind --site dist",
    "build:search": "astro build && pagefind --site dist",
    "preview:search": "npm run build:search && astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "@fontsource-variable/hanken-grotesk": "^5.2.8",
    "@fontsource/atkinson-hyperlegible": "^5.2.8",
    "astro": "^6.4.7"
  },
  "devDependencies": {
    "pagefind": "^1.5.2"
  }
}
